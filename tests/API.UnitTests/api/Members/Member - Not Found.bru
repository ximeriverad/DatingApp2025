meta {
  name: Member - Not Found
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/members/123456
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("Should return not found", function() {
    expect(res.getStatus()).to.equal(404);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}