meta {
  name: Member - Unauthorized
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/members
  body: none
  auth: none
}

tests {
  test("Should return unauthorized", function() {
    expect(res.getStatus()).to.equal(401);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}