meta {
  name: Login - Unauthorized Bad Password
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/account/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "arenita@test.com",
    "password": ""
  }
}

tests {
  test("Should return unauthorized", function() {
    expect(res.getStatus()).to.equal(401);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}