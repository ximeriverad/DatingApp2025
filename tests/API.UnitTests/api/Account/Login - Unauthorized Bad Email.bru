meta {
  name: Login - Unauthorized Bad Email
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/account/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test@test.com",
    "password": ""
  }
}

tests {
  test("Should return unauthorized", function() {
    expect(res.getStatus()).to.equal(401);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}